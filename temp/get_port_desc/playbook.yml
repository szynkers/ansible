---
- name: Export Cisco SMB interface descriptions to CSV
  hosts: switches
  gather_facts: no

  tasks:
    - name: Run "show system"
      community.ciscosmb.command:
        commands:
          - show system
      register: run_cfg

